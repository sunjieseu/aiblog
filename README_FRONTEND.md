# 技术博客 - Tech Blog

一个现代化的技术博客前端应用，使用 React + TypeScript + Vite + Tailwind CSS 构建。

## 功能特性

- 📝 文章列表展示（首页）
- 📖 文章详情查看
- ✍️ 创建和编辑文章
- 🗑️ 删除文章
- 🎨 现代化响应式设计
- 🚀 快速的页面加载

## 技术栈

- **前端框架**: React 18.3.1
- **开发语言**: TypeScript 5.6.3
- **构建工具**: Vite 5.4.10
- **路由管理**: React Router DOM 6.28.0
- **HTTP 客户端**: Axios 1.7.7
- **UI 框架**: Tailwind CSS 3.4.14
- **图标库**: Lucide React 0.454.0
- **日期处理**: date-fns 4.1.0

## 项目结构

```
gitee_io/
├── src/
│   ├── components/       # 可复用组件
│   │   ├── Navbar.tsx   # 导航栏组件
│   │   └── Footer.tsx   # 页脚组件
│   ├── pages/           # 页面组件
│   │   ├── Home.tsx     # 首页（文章列表）
│   │   ├── PostDetail.tsx  # 文章详情页
│   │   └── PostForm.tsx # 文章表单页（创建/编辑）
│   ├── services/        # API 服务层
│   │   └── api.ts       # API 接口封装
│   ├── types/           # TypeScript 类型定义
│   │   └── index.ts     # 类型定义文件
│   ├── App.tsx          # 主应用组件
│   ├── main.tsx         # 应用入口
│   └── index.css        # 全局样式
├── backend/             # 后端服务（Node.js + Express）
├── public/              # 静态资源
├── index.html           # HTML 入口文件
├── vite.config.ts       # Vite 配置
├── tailwind.config.js   # Tailwind CSS 配置
├── tsconfig.json        # TypeScript 配置
└── package.json         # 项目依赖配置
```

## 快速开始

### 前置要求

- Node.js 18+ 
- npm 或 yarn

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

应用将在 `http://localhost:5173/gitee_io/` 启动

### 构建生产版本

```bash
npm run build
```

### 预览生产构建

```bash
npm run preview
```

## 后端服务

前端需要与后端服务配合使用。后端服务默认运行在 `http://localhost:7862`

### 启动后端服务

```bash
cd backend
npm install
npm start
```

## 环境变量

在项目根目录创建 `.env` 文件：

```env
VITE_API_URL=http://localhost:7862/api
```

## API 接口

### 文章接口

- `GET /api/posts` - 获取所有文章
- `GET /api/posts/:id` - 获取单个文章
- `POST /api/posts` - 创建新文章
- `PUT /api/posts/:id` - 更新文章
- `DELETE /api/posts/:id` - 删除文章

### 认证接口

- `POST /api/auth/register` - 用户注册
- `POST /api/auth/login` - 用户登录

## 页面路由

- `/` - 首页（文章列表）
- `/posts/:id` - 文章详情页
- `/posts/new` - 创建新文章
- `/posts/:id/edit` - 编辑文章

## 设计特点

- **响应式设计**: 适配桌面端和移动端
- **现代化 UI**: 使用 Tailwind CSS 构建的简洁美观界面
- **卡片式布局**: 文章以卡片形式展示
- **悬停效果**: 丰富的交互动画
- **加载状态**: 友好的加载和错误提示
- **表单验证**: 基本的表单验证

## 开发说明

### 添加新页面

1. 在 `src/pages/` 目录下创建新的页面组件
2. 在 `src/App.tsx` 中添加路由配置
3. 在导航栏中添加链接（如果需要）

### 修改样式

- 全局样式在 `src/index.css` 中修改
- 组件样式使用 Tailwind CSS 类名
- 自定义颜色在 `tailwind.config.js` 中配置

### API 调用

所有 API 调用都封装在 `src/services/api.ts` 中，使用时直接导入：

```typescript
import { postsApi } from '../services/api';

// 获取所有文章
const posts = await postsApi.getAll();

// 创建文章
const newPost = await postsApi.create({
  title: '标题',
  content: '内容',
  authorId: 1
});
```

## 部署

### Gitee Pages 部署

1. 构建项目：`npm run build`
2. 将 `dist` 目录上传到 Gitee 仓库
3. 在 Gitee 设置中启用 Pages 服务
4. 设置部署目录为 `/dist`

### 其他平台

项目可以部署到任何支持静态网站托管的平台，如：
- Vercel
- Netlify
- GitHub Pages
- Cloudflare Pages

## 许可证

MIT License

## 贡献

欢迎提交 Issue 和 Pull Request！
